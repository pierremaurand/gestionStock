/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseCommandeClientResponse } from '../../models/page-response-commande-client-response';

export interface FindAll6$Params {
  page?: number;
  size?: number;
}

export function findAll6(http: HttpClient, rootUrl: string, params?: FindAll6$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseCommandeClientResponse>> {
  const rb = new RequestBuilder(rootUrl, findAll6.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseCommandeClientResponse>;
    })
  );
}

findAll6.PATH = '/commandes-client';
